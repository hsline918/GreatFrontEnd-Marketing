export const animation = {
  animation: {
    "snackbar-slide-in": "slideIn 1s ease-in forwards",
    "snackbar-slide-out": "slideOut 1s ease-in forwards",
    //transition timing function use CSS
    //在動畫定義中添加 animation-fill-mode: forwards;。這會讓元素在動畫結束後保持最後一個關鍵幀的狀態。（解決閃一下的問題)
  },
};
